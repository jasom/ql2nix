
{ buildLispPackage, stdenv, fetchurl, lisp-project_repl-utilities, 
   
  sbcl, clisp, ccl,  
  system ? builtins.currentSystem }:

let
  pkgs = import <nixpkgs> { inherit system; };
  #buildLispPackage = pkgs.callPackage ./lisp-builder/default.nix pkgs.sbcl;
in
  buildLispPackage {
      propagatedBuildInputs = [   ];
      inherit stdenv;
      systemName = "repl-utilities";
      
      sourceProject = "${lisp-project_repl-utilities}";
      patches = [];
      lisp_dependencies = "";
      name = "lisp_repl-utilities-20150608-git";
      #lisp = "${pkgs.sbcl}/bin/sbcl";
      lisp_implementations = [ "${pkgs.sbcl}" "${pkgs.clisp}" "${pkgs.ccl}" ];
    }
