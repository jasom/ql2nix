
{ buildLispPackage, stdenv, fetchurl, lisp-project_cl-quickcheck, 
   
  sbcl, clisp, ccl,  
  system ? builtins.currentSystem }:

let
  pkgs = import <nixpkgs> { inherit system; };
  #buildLispPackage = pkgs.callPackage ./lisp-builder/default.nix pkgs.sbcl;
in
  buildLispPackage {
      propagatedBuildInputs = [   ];
      inherit stdenv;
      systemName = "cl-quickcheck";
      
      sourceProject = "${lisp-project_cl-quickcheck}";
      patches = [];
      lisp_dependencies = "";
      name = "lisp_cl-quickcheck-20161208-git";
      #lisp = "${pkgs.sbcl}/bin/sbcl";
      lisp_implementations = [ "${pkgs.sbcl}" "${pkgs.clisp}" "${pkgs.ccl}" ];
    }
