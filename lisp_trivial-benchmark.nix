
{ buildLispPackage, stdenv, fetchurl, lisp-project_trivial-benchmark, 
   lisp_alexandria,  
  ccl, clisp, sbcl,  
  system ? builtins.currentSystem }:

let
  pkgs = import <nixpkgs> { inherit system; };
  #buildLispPackage = pkgs.callPackage ./lisp-builder/default.nix pkgs.sbcl;
in
  buildLispPackage {
      propagatedBuildInputs = [ lisp_alexandria  ];
      inherit stdenv;
      systemName = "trivial-benchmark";
      
      sourceProject = "${lisp-project_trivial-benchmark}";
      patches = [];
      lisp_dependencies = "${lisp_alexandria}";
      name = "lisp_trivial-benchmark-20170830-git";
      #lisp = "${pkgs.sbcl}/bin/sbcl";
      lisp_implementations = [ "${pkgs.ccl}" "${pkgs.clisp}" "${pkgs.sbcl}" ];
    }
